import 'package:flutter/material.dart';

/// Placeholder for AssessmentHomeScreen.
/// Full implementation is in task 7.5.
class AssessmentHomeScreen extends StatelessWidget {
  const AssessmentHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Assessment')),
      body: const Center(child: Text('Assessment Home — coming soon')),
    );
  }
}
