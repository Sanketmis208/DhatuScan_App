import 'package:flutter/material.dart';

/// Placeholder for Section2Screen.
/// Full implementation is in task 9.4.
class Section2Screen extends StatelessWidget {
  const Section2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Section 2')),
      body: const Center(child: Text('Section 2 — coming soon')),
    );
  }
}
