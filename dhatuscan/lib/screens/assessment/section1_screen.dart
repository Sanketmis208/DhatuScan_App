import 'package:flutter/material.dart';

/// Placeholder for Section1Screen.
/// Full implementation is in task 7.6.
class Section1Screen extends StatelessWidget {
  const Section1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Section 1')),
      body: const Center(child: Text('Section 1 — coming soon')),
    );
  }
}
